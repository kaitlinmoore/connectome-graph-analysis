# connectome-graph-analysis
A from-scratch graph-algorithms library applied to biological connectomes. The library is the spine; the connectome is the substrate. Every algorithm is implemented by hand and validated against NetworkX on the identical graph. The analytical payoff is a triangulation result.
